import AppKit

// Constant values all in one place for adjusting the default layout and style of the app.
struct DesignConstants {
    
    // Window styling
    static let windowFrameWidth = CGFloat(658)
    static let windowFrameHeight = CGFloat(658)
    static let windowBackgroundColor = NSColor(deviceRed: 246/255, green: 244/255, blue: 241/255, alpha: 1.0)
    
    // Grid styling
    static let gridFrameWidth = 330
    static let gridFrameHeight = 568
    
}
